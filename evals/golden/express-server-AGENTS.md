# AGENTS.md

## Project

`express-server` is a minimal API server built with `JavaScript` on `Node.js` using `Express` and `MongoDB`.

## Commands

```shell
# Develop
npm start              # start the server via `node src/server.js`

# Validate
npm test               # placeholder test script; currently exits with an error
```

## Constraints

- **Generated files**: do not edit `dist/`, `build/`, or lock files directly

## Quality

- **CI**: no CI workflow was found in the repository metadata provided; rely on the available `npm` scripts before merge
