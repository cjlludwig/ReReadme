---
name: Feature request
about: Propose a concrete addition or behavioral change
title: "feat: "
labels: enhancement
---

<!-- HINT
> Fill in all required sections. Remove placeholder text before submitting.
> A well-specified feature request gives whoever is implementing this enough context to build and test it without follow-up questions.
-->

## Problem

<!--
Required. Describe the pain point this solves — not the solution.
Focus on the scenario where the current behavior falls short.
-->

> _e.g. When running rereadme in CI on a monorepo, every package overwrites the same README.md path. There is no way to specify a custom output location per package._

## Proposed Solution

<!--
Required. Describe the intended behavior concretely.
Avoid implementation details — describe what the tool should do, not how.
-->

> _e.g. Add a `--output <path>` flag that writes the generated README to the specified file instead of the default README.md._

## Example Usage

<!--
Required. Show the CLI invocation or API call that demonstrates the intended UX.
HINT:
  rereadme --output docs/README.generated.md
  # → writes output to docs/README.generated.md, not README.md
-->

```sh

```

## Acceptance Criteria

<!--
Required. Checklist of testable, observable conditions that define done.
-->

- [ ]
- [ ]
- [ ]
